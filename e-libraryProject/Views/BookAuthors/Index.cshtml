@model IEnumerable<e_libraryProject.Models.BookAuthor>

@{
    ViewData["Title"] = "Book Authors";
}

<div class="container my-4">

    <!-- Page header -->
    <div class="d-flex flex-column flex-sm-row align-items-sm-center justify-content-between gap-2 mb-3">
        <div>
            <h1 class="h3 mb-1">Book Authors</h1>
            <div class="text-muted small">Manage links between books and authors.</div>
        </div>

        <a asp-action="Create" class="btn btn-success">
            + Link Book &amp; Author
        </a>
    </div>

    <!-- Search / Sort -->
    <partial name="_SearchSortBar" model="((string)ViewBag.Search, (string)ViewBag.Sort)" />

    <!-- Table -->
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th class="ps-3">Book</th>
                            <th>Author</th>
                            <th>Added At</th>
                            <th>Note</th>
                            <th class="text-end pe-3" style="width: 280px;">Actions</th>
                        </tr>
                    </thead>

                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td class="ps-3 fw-semibold">
                                        @item.Book?.Title
                                    </td>

                                    <td>
                                        @item.Author?.Name
                                    </td>

                                    <td class="text-muted">
                                        @Html.DisplayFor(m => item.AddedAt)
                                    </td>

                                    <td>
                                        @if (!string.IsNullOrWhiteSpace(item.Note))
                                        {
                                            @Html.DisplayFor(m => item.Note)
                                        }
                                        else
                                        {
                                            <span class="text-muted">—</span>
                                        }
                                    </td>

                                    <td class="text-end pe-3">
                                        <div class="btn-group btn-group-sm" role="group" aria-label="Link actions">
                                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info">
                                                Details
                                            </a>
                                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-primary">
                                                Edit
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">
                                                Delete
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="5" class="p-4">
                                    <div class="alert alert-secondary mb-0" role="alert">
                                        No links found. Try adjusting your search, or create a new link.
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
        </div>
    </div>

</div>
